class conditionTest {
    public static void main(String args[]) {
        String status1;
        int rank = 3;
        status = (rank == 1) ? "Done" : "Pending";
        System.out.println(status1);
    }
}
